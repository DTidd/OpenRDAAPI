/* finvirt1.c - Makes FINMGT EXPENDITURE 1 Virtual Fields */
#include <cstdio>





#include <rptgen.hpp>
/*ADDLIB rpt */
/*ADDLIB mix */
/*ADDLIB olh */
/*ADDLIB sec */
/*ADDLIB trans */
/*ADDLIB gui */
/*ADDLIB nrd */
/*ADDLIB misc */
/*ADDLIB ctpf */

extern RDAfinmgt *finsetup;
extern char *getmonth(int);
#define QUARTER_POSTED
#define QUARTER_DISTRIBUTED
#define QUARTER_UNDISTRIBUTED

void FINMGTCreateEV1()
{
	DFvirtual *d=NULL;
	DFincvir *i;
	char temp[15000];

	d=DFvirtualNEW("FINMGT","EXPENDITURE TYPE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=SHORTV;
	d->length=1;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"0");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the numerical value of the Expenditure Account Type.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","REVENUE TYPE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=SHORTV;
	d->length=1;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"1");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the numerical value of the Revenue Account Type.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","BALANCE SHEET TYPE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=SHORTV;
	d->length=1;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"2");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the numerical value of the Balance Sheet Account Type.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL POSTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR APPROPRIATIONS]",
		"[FINMGT][FINEYR][JANUARY APPROPRIATIONS]",
		"[FINMGT][FINEYR][FEBRUARY APPROPRIATIONS]",
		"[FINMGT][FINEYR][MARCH APPROPRIATIONS]",
		"[FINMGT][FINEYR][APRIL APPROPRIATIONS]",
		"[FINMGT][FINEYR][MAY APPROPRIATIONS]",
		"[FINMGT][FINEYR][JUNE APPROPRIATIONS]",
		"[FINMGT][FINEYR][JULY APPROPRIATIONS]",
		"[FINMGT][FINEYR][AUGUST APPROPRIATIONS]",
		"[FINMGT][FINEYR][SEPTEMBER APPROPRIATIONS]",
		"[FINMGT][FINEYR][OCTOBER APPROPRIATIONS]",
		"[FINMGT][FINEYR][NOVEMBER APPROPRIATIONS]",
		"[FINMGT][FINEYR][DECEMBER APPROPRIATIONS]",
		"[FINMGT][FINEYR][POST YEAR APPROPRIATIONS]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual posted appropriations, i.e. the total posted Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL DISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JANUARY DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][FEBRUARY DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][MARCH DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][APRIL DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][MAY DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JUNE DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JULY DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][AUGUST DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][OCTOBER DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][NOVEMBER DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][DECEMBER DISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][POST YEAR DISTRIBUTED APPROPRIATIONS]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual distributed appropriations, i.e. the total distributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL UNDISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JANUARY UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][MARCH UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][APRIL UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][MAY UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JUNE UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][JULY UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][AUGUST UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED APPROPRIATIONS]",
		"[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED APPROPRIATIONS]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual undistributed appropriations, i.e. the total undistributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL POSTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR ENCUMBRANCES]",
		"[FINMGT][FINEYR][JANUARY ENCUMBRANCES]",
		"[FINMGT][FINEYR][FEBRUARY ENCUMBRANCES]",
		"[FINMGT][FINEYR][MARCH ENCUMBRANCES]",
		"[FINMGT][FINEYR][APRIL ENCUMBRANCES]",
		"[FINMGT][FINEYR][MAY ENCUMBRANCES]",
		"[FINMGT][FINEYR][JUNE ENCUMBRANCES]",
		"[FINMGT][FINEYR][JULY ENCUMBRANCES]",
		"[FINMGT][FINEYR][AUGUST ENCUMBRANCES]",
		"[FINMGT][FINEYR][SEPTEMBER ENCUMBRANCES]",
		"[FINMGT][FINEYR][OCTOBER ENCUMBRANCES]",
		"[FINMGT][FINEYR][NOVEMBER ENCUMBRANCES]",
		"[FINMGT][FINEYR][DECEMBER ENCUMBRANCES]",
		"[FINMGT][FINEYR][POST YEAR ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual posted encumbrances, i.e. the total posted Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL DISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JANUARY DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][FEBRUARY DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][MARCH DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][APRIL DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][MAY DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JUNE DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JULY DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][AUGUST DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][OCTOBER DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][NOVEMBER DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][DECEMBER DISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][POST YEAR DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual distributed encumbrances, i.e. the total distributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL UNDISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JANUARY UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][MARCH UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][APRIL UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][MAY UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JUNE UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][JULY UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][AUGUST UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED ENCUMBRANCES]",
		"[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual undistributed encumbrances, i.e. the total undistributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL POSTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR EXPENDITURES]",
		"[FINMGT][FINEYR][JANUARY EXPENDITURES]",
		"[FINMGT][FINEYR][FEBRUARY EXPENDITURES]",
		"[FINMGT][FINEYR][MARCH EXPENDITURES]",
		"[FINMGT][FINEYR][APRIL EXPENDITURES]",
		"[FINMGT][FINEYR][MAY EXPENDITURES]",
		"[FINMGT][FINEYR][JUNE EXPENDITURES]",
		"[FINMGT][FINEYR][JULY EXPENDITURES]",
		"[FINMGT][FINEYR][AUGUST EXPENDITURES]",
		"[FINMGT][FINEYR][SEPTEMBER EXPENDITURES]",
		"[FINMGT][FINEYR][OCTOBER EXPENDITURES]",
		"[FINMGT][FINEYR][NOVEMBER EXPENDITURES]",
		"[FINMGT][FINEYR][DECEMBER EXPENDITURES]",
		"[FINMGT][FINEYR][POST YEAR EXPENDITURES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual posted expenditures, i.e. the total posted Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL DISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JANUARY DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][FEBRUARY DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][MARCH DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][APRIL DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][MAY DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JUNE DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JULY DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][AUGUST DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][OCTOBER DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][NOVEMBER DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][DECEMBER DISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][POST YEAR DISTRIBUTED EXPENDITURES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual distributed expenditures, i.e. the total distributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL UNDISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s+%s",
		"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JANUARY UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][MARCH UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][APRIL UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][MAY UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JUNE UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][JULY UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][AUGUST UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED EXPENDITURES]",
		"[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED EXPENDITURES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual undistributed expenditures, i.e. the total undistributed Previous Year, January, February, March, April, May, June, July, August, September, October, November, December, and Post Year.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER APPROPRIATIONS TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR APPROPRIATIONS]+[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2),getmonth(finsetup->month),
	getmonth(finsetup->month+1),getmonth(finsetup->month+2),
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter appropriations, i.e. the total posted, distributed and undistributed for July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER APPROPRIATIONS TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5),getmonth(finsetup->month+3),
	getmonth(finsetup->month+4),getmonth(finsetup->month+5),
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter appropriations, i.e. the total posted, distributed and undistributed for October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER APPROPRIATIONS TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8),getmonth(finsetup->month+6),
	getmonth(finsetup->month+7),getmonth(finsetup->month+8),
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter appropriations, i.e. the total posted, distributed and undistributed for January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER APPROPRIATIONS TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED APPROPRIATIONS]",
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11),getmonth(finsetup->month+9),
	getmonth(finsetup->month+10),getmonth(finsetup->month+11),
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter appropriations, i.e. the total posted, distributed and undistributed for April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);

	d=DFvirtualNEW("FINMGT","FIRST QUARTER ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR ENCUMBRANCES]+[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2),getmonth(finsetup->month),
	getmonth(finsetup->month+1),getmonth(finsetup->month+2),
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter encumbrances, i.e. the total posted, distributed and undistributed for July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5),getmonth(finsetup->month+3),
	getmonth(finsetup->month+4),getmonth(finsetup->month+5),
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter encumbrances, i.e. the total posted, distributed and undistributed for October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8),getmonth(finsetup->month+6),
	getmonth(finsetup->month+7),getmonth(finsetup->month+8),
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter encumbrances, i.e. the total posted, distributed and undistributed for January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED ENCUMBRANCES]",
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11),getmonth(finsetup->month+9),
	getmonth(finsetup->month+10),getmonth(finsetup->month+11),
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter encumbrances, i.e. the total posted, distributed and undistributed for April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);

	d=DFvirtualNEW("FINMGT","FIRST QUARTER EXPENDITURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR EXPENDITURES]+[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2),getmonth(finsetup->month),
	getmonth(finsetup->month+1),getmonth(finsetup->month+2),
	getmonth(finsetup->month),getmonth(finsetup->month+1),
	getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter expenditures, i.e. the total posted, distributed and undistributed for July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER EXPENDITURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5),getmonth(finsetup->month+3),
	getmonth(finsetup->month+4),getmonth(finsetup->month+5),
	getmonth(finsetup->month+3),getmonth(finsetup->month+4),
	getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter expenditures, i.e. the total posted, distributed and undistributed for October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER EXPENDITURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8),getmonth(finsetup->month+6),
	getmonth(finsetup->month+7),getmonth(finsetup->month+8),
	getmonth(finsetup->month+6),getmonth(finsetup->month+7),
	getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter expenditures, i.e. the total posted, distributed and undistributed for January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER EXPENDITURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][POST YEAR EXPENDITURES]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED EXPENDITURES]",
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11),getmonth(finsetup->month+9),
	getmonth(finsetup->month+10),getmonth(finsetup->month+11),
	getmonth(finsetup->month+9),getmonth(finsetup->month+10),
	getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter expenditures, i.e. the total posted, distributed and undistributed for April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
#ifdef QUARTER_POSTED
	d=DFvirtualNEW("FINMGT","FIRST QUARTER POSTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter posted appropriations, i.e. the total posted July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER POSTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter posted appropriations, i.e. the total posted October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER POSTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter posted appropriations, i.e. the total posted January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER POSTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][%s APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR APPROPRIATIONS]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter posted appropriations, i.e. the total posted April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER POSTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter posted encumbrances, i.e. the total posted July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER POSTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter posted encumbrances, i.e. the total posted October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER POSTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter posted encumbrances, i.e. the total posted January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER POSTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][%s ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR ENCUMBRANCES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter posted encumbrances, i.e. the total posted April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER POSTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter posted expenditures, i.e. the total posted July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER POSTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter posted expenditures, i.e. the total posted October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER POSTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter posted expenditures, i.e. the total posted January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER POSTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][%s EXPENDITURES]+[FINMGT][FINEYR][POST YEAR EXPENDITURES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter posted expenditures, i.e. the total posted April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
#endif
#ifdef QUARTER_DISTRIBUTED
	d=DFvirtualNEW("FINMGT","FIRST QUARTER DISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter distributed appropriations, i.e. the total distributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER DISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter distributed appropriations, i.e. the total distributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER DISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter distributed appropriations, i.e. the total distributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER DISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s DISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter distributed appropriations, i.e. the total distributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER DISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter distributed encumbrances, i.e. the total distributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER DISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter distributed encumbrances, i.e. the total distributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER DISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter distributed encumbrances, i.e. the total distributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER DISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s DISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter distributed encumbrances, i.e. the total distributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER DISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter distributed expenditures, i.e. the total distributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER DISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter distributed expenditures, i.e. the total distributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER DISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter distributed expenditures, i.e. the total distributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER DISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s DISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][POST YEAR DISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter distributed expenditures, i.e. the total distributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
#endif
#ifdef QUARTER_UNDISTRIBUTED
	d=DFvirtualNEW("FINMGT","FIRST QUARTER UNDISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter undistributed appropriations, i.e. the total undistributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER UNDISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter undistributed appropriations, i.e. the total undistributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER UNDISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter undistributed appropriations, i.e. the total undistributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER UNDISTRIBUTED APPROPRIATIONS");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][%s UNDISTRIBUTED APPROPRIATIONS]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED APPROPRIATIONS]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter undistributed appropriations, i.e. the total undistributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER UNDISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter undistributed encumbrances, i.e. the total undistributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER UNDISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter undistributed encumbrances, i.e. the total undistributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER UNDISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter undistributed encumbrances, i.e. the total undistributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER UNDISTRIBUTED ENCUMBRANCES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][%s UNDISTRIBUTED ENCUMBRANCES]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED ENCUMBRANCES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter undistributed encumbrances, i.e. the total undistributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FIRST QUARTER UNDISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",getmonth(finsetup->month),getmonth(finsetup->month+1),getmonth(finsetup->month+2));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total first quarter undistributed expenditures, i.e. the total undistributed July, August, and September.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SECOND QUARTER UNDISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+3),getmonth(finsetup->month+4),getmonth(finsetup->month+5));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total second quarter undistributed expenditures, i.e. the total undistributed October, November, and December.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","THIRD QUARTER UNDISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+6),getmonth(finsetup->month+7),getmonth(finsetup->month+8));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total third quarter undistributed expenditures, i.e. the total undistributed January, February, and March.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FOURTH QUARTER UNDISTRIBUTED EXPENDITURES");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=FALSE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][%s UNDISTRIBUTED EXPENDITURES]+[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED EXPENDITURES]",getmonth(finsetup->month+9),getmonth(finsetup->month+10),getmonth(finsetup->month+11));
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total fourth quarter undistributed expenditures, i.e. the total undistributed April, May, and June.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
#endif
	d=DFvirtualNEW("FINMGT","ANNUAL EXPENDITURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED EXPENDITURES]+[ANNUAL DISTRIBUTED EXPENDITURES]+[ANNUAL UNDISTRIBUTED EXPENDITURES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual expenditures, i.e. the total posted expenditures, distributed expenditures, and undistributed expenditures.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED ENCUMBRANCES]+[ANNUAL DISTRIBUTED ENCUMBRANCES]+[ANNUAL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual encumbrances, i.e. the total posted encumbrances, distributed encumbrances, and undistributed encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL APPROPRIATIONS TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED APPROPRIATIONS]+[ANNUAL DISTRIBUTED APPROPRIATIONS]+[ANNUAL UNDISTRIBUTED APPROPRIATIONS]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual appropriations, posted, distributed, and undistributed for all months in the fiscal year");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED APPROPRIATIONS]-[ANNUAL POSTED EXPENDITURES]-[ANNUAL POSTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual posted available balance calculated by the total annual posted appropriations minus the posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL DISTRIBUTED APPROPRIATIONS]-[ANNUAL DISTRIBUTED EXPENDITURES]-[ANNUAL DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual distributed available balance calculated by the total annual distributed appropriations minus the distributd expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL UNDISTRIBUTED APPROPRIATIONS]-[ANNUAL UNDISTRIBUTED EXPENDITURES]-[ANNUAL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total undistributed annual available balance calculated by the total annual undistributed appropriations minus the undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED APPROPRIATIONS]-[ANNUAL POSTED EXPENDITURES]-[ANNUAL DISTRIBUTED EXPENDITURES]-[ANNUAL UNDISTRIBUTED EXPENDITURES]-[ANNUAL POSTED ENCUMBRANCES]-[ANNUAL DISTRIBUTED ENCUMBRANCES]-[ANNUAL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual available for use balance calculated by the total annual posted appropriations minus posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","ANNUAL ENCUMBRANCES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[ANNUAL POSTED ENCUMBRANCES]+[ANNUAL DISTRIBUTED ENCUMBRANCES]+[ANNUAL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the total annual encumbrances, i.e. the total posted encumbrances, distributed encumbrances, and undistributed encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","PRE-YEAR POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR APPROPRIATIONS]-[FINMGT][FINEYR][PRE-YEAR EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Pre-Year posted available balance which is calculated by the difference between the Pre-Year posted appropriations and the sum of the Pre-Year posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","POST YEAR POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][POST YEAR APPROPRIATIONS]-[FINMGT][FINEYR][POST YEAR EXPENDITURES]-[FINMGT][FINEYR][POST YEAR ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Post Year posted available balance which is calculated by the difference between the Post Year posted appropriations and the sum of the Post Year posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JANUARY POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JANUARY APPROPRIATIONS]-[FINMGT][FINEYR][JANUARY EXPENDITURES]-[FINMGT][FINEYR][JANUARY ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the January posted available balance which is calculated by the difference between the January posted appropriations and the sum of the January posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FEBRUARY POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][FEBRUARY APPROPRIATIONS]-[FINMGT][FINEYR][FEBRUARY EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the February posted available balance which is calculated by the difference between the February posted appropriations and the sum of the February posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FEBRUARY POSTED EXPENDTURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][FEBRUARY APPROPRIATIONS]-[FINMGT][FINEYR][FEBRUARY EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the February posted available balance which is calculated by the difference between the February posted appropriations and the sum of the February posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MARCH POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MARCH APPROPRIATIONS]-[FINMGT][FINEYR][MARCH EXPENDITURES]-[FINMGT][FINEYR][MARCH ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the March posted available balance which is calculated by the difference between the March posted appropriations and the sum of the March posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","APRIL POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][APRIL APPROPRIATIONS]-[FINMGT][FINEYR][APRIL EXPENDITURES]-[FINMGT][FINEYR][APRIL ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the April posted available balance which is calculated by the difference between the April posted appropriations and the sum of the April posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MAY POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MAY APPROPRIATIONS]-[FINMGT][FINEYR][MAY EXPENDITURES]-[FINMGT][FINEYR][MAY ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the May posted available balance which is calculated by the difference between the May posted appropriations and the sum of the May posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JUNE POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JUNE APPROPRIATIONS]-[FINMGT][FINEYR][JUNE EXPENDITURES]-[FINMGT][FINEYR][JUNE ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the June posted available balance which is calculated by the difference between the June posted appropriations and the sum of the June posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JULY POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JULY APPROPRIATIONS]-[FINMGT][FINEYR][JULY EXPENDITURES]-[FINMGT][FINEYR][JULY ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the July posted available balance which is calculated by the difference between the July posted appropriations and the sum of the July posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","AUGUST POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][AUGUST APPROPRIATIONS]-[FINMGT][FINEYR][AUGUST EXPENDITURES]-[FINMGT][FINEYR][AUGUST ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the August posted available balance which is calculated by the difference between the August posted appropriations and the sum of the August posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SEPTEMBER POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][SEPTEMBER APPROPRIATIONS]-[FINMGT][FINEYR][SEPTEMBER EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the September posted available balance which is calculated by the difference between the September posted appropriations and the sum of the September posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","OCTOBER POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][OCTOBER APPROPRIATIONS]-[FINMGT][FINEYR][OCTOBER EXPENDITURES]-[FINMGT][FINEYR][OCTOBER ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the October posted available balance which is calculated by the difference between the October posted appropriations and the sum of the October posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","NOVEMBER POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][NOVEMBER APPROPRIATIONS]-[FINMGT][FINEYR][NOVEMBER EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the November posted available balance which is calculated by the difference between the November posted appropriations and the sum of the November posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","DECEMBER POSTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][DECEMBER APPROPRIATIONS]-[FINMGT][FINEYR][DECEMBER EXPENDITURES]-[FINMGT][FINEYR][DECEMBER ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the December posted available balance which is calculated by the difference between the December posted appropriations and the sum of the December posted expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","PRE-YEAR DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Pre-Year distributed available balance which is calculated by the difference between the Pre-Year distributed appropriations and the sum of the Pre-Year distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","POST YEAR DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][POST YEAR DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][POST YEAR DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][POST YEAR DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Post Year distributed available balance which is calculated by the difference between the Post Year distributed appropriations and the sum of the Post Year distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JANUARY DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JANUARY DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JANUARY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JANUARY DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the January distributed available balance which is calculated by the difference between the January distributed appropriations and the sum of the January distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FEBRUARY DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][FEBRUARY DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][FEBRUARY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the February distributed available balance which is calculated by the difference between the February distributed appropriations and the sum of the February distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MARCH DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MARCH DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][MARCH DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MARCH DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the March distributed available balance which is calculated by the difference between the March distributed appropriations and the sum of the March distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","APRIL DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][APRIL DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][APRIL DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][APRIL DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the April distributed available balance which is calculated by the difference between the April distributed appropriations and the sum of the April distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MAY DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MAY DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][MAY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MAY DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the May distributed available balance which is calculated by the difference between the May distributed appropriations and the sum of the May distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JUNE DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JUNE DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JUNE DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JUNE DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the June distributed available balance which is calculated by the difference between the June distributed appropriations and the sum of the June distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JULY DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JULY DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JULY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JULY DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the July distributed available balance which is calculated by the difference between the July distributed appropriations and the sum of the July distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","AUGUST DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][AUGUST DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][AUGUST DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][AUGUST DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the August distributed available balance which is calculated by the difference between the August distributed appropriations and the sum of the August distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SEPTEMBER DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the September distributed available balance which is calculated by the difference between the September distributed appropriations and the sum of the September distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","OCTOBER DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][OCTOBER DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][OCTOBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][OCTOBER DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the October distributed available balance which is calculated by the difference between the October distributed appropriations and the sum of the October distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","NOVEMBER DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][NOVEMBER DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][NOVEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the November distributed available balance which is calculated by the difference between the November distributed appropriations and the sum of the November distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","DECEMBER DISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][DECEMBER DISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][DECEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][DECEMBER DISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the December distributed available balance which is calculated by the difference between the December distributed appropriations and the sum of the December distributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","PRE-YEAR UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Pre-Year undistributed available balance which is calculated by the difference between the Pre-Year undistributed appropriations and the sum of the Pre-Year undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","POST YEAR UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Post Year undistributed available balance which is calculated by the difference between the Post Year undistributed appropriations and the sum of the Post Year undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JANUARY UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JANUARY UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JANUARY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JANUARY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the January undistributed available balance which is calculated by the difference between the January undistributed appropriations and the sum of the January undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FEBRUARY UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the February undistributed available balance which is calculated by the difference between the February undistributed appropriations and the sum of the February undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MARCH UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MARCH UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][MARCH UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MARCH UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the March undistributed available balance which is calculated by the difference between the March undistributed appropriations and the sum of the March undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","APRIL UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][APRIL UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][APRIL UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][APRIL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the April undistributed available balance which is calculated by the difference between the April undistributed appropriations and the sum of the April undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MAY UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MAY UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][MAY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MAY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the May undistributed available balance which is calculated by the difference between the May undistributed appropriations and the sum of the May undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JUNE UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JUNE UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JUNE UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JUNE UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the June undistributed available balance which is calculated by the difference between the June undistributed appropriations and the sum of the June undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JULY UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JULY UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][JULY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JULY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the July undistributed available balance which is calculated by the difference between the July undistributed appropriations and the sum of the July undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","AUGUST UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][AUGUST UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][AUGUST UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][AUGUST UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the August undistributed available balance which is calculated by the difference between the August undistributed appropriations and the sum of the August undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SEPTEMBER UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the September undistributed available balance which is calculated by the difference between the September undistributed appropriations and the sum of the September undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","OCTOBER UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the October undistributed available balance which is calculated by the difference between the October undistributed appropriations and the sum of the October undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","NOVEMBER UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the November undistributed available balance which is calculated by the difference between the November undistributed appropriations and the sum of the November undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","DECEMBER UNDISTRIBUTED AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED APPROPRIATIONS]-[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the December undistributed available balance which is calculated by the difference between the December undistributed appropriations and the sum of the December undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","PRE-YEAR AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][PRE-YEAR APPROPRIATIONS]-[FINMGT][FINEYR][PRE-YEAR EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][PRE-YEAR ENCUMBRANCES]-[FINMGT][FINEYR][PRE-YEAR DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][PRE-YEAR UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Pre-Year available balance which is calculated by the difference between the Pre-Year posted appropriations and the sum of the Pre-Year posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","POST YEAR AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][POST YEAR APPROPRIATIONS]-[FINMGT][FINEYR][POST YEAR EXPENDITURES]-[FINMGT][FINEYR][POST YEAR DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][POST YEAR ENCUMBRANCES]-[FINMGT][FINEYR][POST YEAR DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][POST YEAR UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the Post Year available balance which is calculated by the difference between the Post Year posted appropriations and the sum of the Post Year posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JANUARY AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JANUARY APPROPRIATIONS]-[FINMGT][FINEYR][JANUARY EXPENDITURES]-[FINMGT][FINEYR][JANUARY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JANUARY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JANUARY ENCUMBRANCES]-[FINMGT][FINEYR][JANUARY DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][JANUARY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the January available balance which is calculated by the difference between the January posted appropriations and the sum of the January posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","FEBRUARY EXPENDTURES TOTAL");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][FEBRUARY APPROPRIATIONS]-[FINMGT][FINEYR][FEBRUARY EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][FEBRUARY ENCUMBRANCES]-[FINMGT][FINEYR][FEBRUARY DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][FEBRUARY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the February available balance which is calculated by the difference between the February posted appropriations and the sum of the February posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MARCH AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MARCH APPROPRIATIONS]-[FINMGT][FINEYR][MARCH EXPENDITURES]-[FINMGT][FINEYR][MARCH DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MARCH UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MARCH ENCUMBRANCES]-[FINMGT][FINEYR][MARCH DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][MARCH UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the March available balance which is calculated by the difference between the March posted appropriations and the sum of the March posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","APRIL AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][APRIL APPROPRIATIONS]-[FINMGT][FINEYR][APRIL EXPENDITURES]-[FINMGT][FINEYR][APRIL DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][APRIL UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][APRIL ENCUMBRANCES]-[FINMGT][FINEYR][APRIL DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][APRIL UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the April available balance which is calculated by the difference between the April posted appropriations and the sum of the April posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","MAY AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][MAY APPROPRIATIONS]-[FINMGT][FINEYR][MAY EXPENDITURES]-[FINMGT][FINEYR][MAY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MAY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][MAY ENCUMBRANCES]-[FINMGT][FINEYR][MAY DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][MAY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the May available balance which is calculated by the difference between the May posted appropriations and the sum of the May posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JUNE AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JUNE APPROPRIATIONS]-[FINMGT][FINEYR][JUNE EXPENDITURES]-[FINMGT][FINEYR][JUNE DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JUNE UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JUNE ENCUMBRANCES]-[FINMGT][FINEYR][JUNE DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][JUNE UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the June available balance which is calculated by the difference between the June posted appropriations and the sum of the June posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","JULY AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][JULY APPROPRIATIONS]-[FINMGT][FINEYR][JULY EXPENDITURES]-[FINMGT][FINEYR][JULY DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JULY UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][JULY ENCUMBRANCES]-[FINMGT][FINEYR][JULY DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][JULY UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the July available balance which is calculated by the difference between the July posted appropriations and the sum of the July posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","AUGUST AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][AUGUST APPROPRIATIONS]-[FINMGT][FINEYR][AUGUST EXPENDITURES]-[FINMGT][FINEYR][AUGUST DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][AUGUST UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][AUGUST ENCUMBRANCES]-[FINMGT][FINEYR][AUGUST DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][AUGUST UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the August available balance which is calculated by the difference between the August posted appropriations and the sum of the August posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","SEPTEMBER AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][SEPTEMBER APPROPRIATIONS]-[FINMGT][FINEYR][SEPTEMBER EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][SEPTEMBER ENCUMBRANCES]-[FINMGT][FINEYR][SEPTEMBER DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][SEPTEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the September available balance which is calculated by the difference between the September posted appropriations and the sum of the September posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","OCTOBER AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][OCTOBER APPROPRIATIONS]-[FINMGT][FINEYR][OCTOBER EXPENDITURES]-[FINMGT][FINEYR][OCTOBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][OCTOBER ENCUMBRANCES]-[FINMGT][FINEYR][OCTOBER DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][OCTOBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the October available balance which is calculated by the difference between the October posted appropriations and the sum of the October posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","NOVEMBER AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][NOVEMBER APPROPRIATIONS]-[FINMGT][FINEYR][NOVEMBER EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][NOVEMBER ENCUMBRANCES]-[FINMGT][FINEYR][NOVEMBER DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][NOVEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the November available balance which is calculated by the difference between the November posted appropriations and the sum of the November posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
	d=DFvirtualNEW("FINMGT","DECEMBER AVAILABLE BALANCE");
	d->incvir=Rmalloc(sizeof(DFincvir));
	i=d->incvir;
	i->module=stralloc("FINMGT");
	i->file=stralloc("FINEYR");
	d->vtype=0;
	d->num=1;
	d->type=DOLLARS;
	d->length=20;
	d->when=0;
	d->avail2rsrc=TRUE;
	memset(temp,0,15000);
	sprintf(temp,"[FINMGT][FINEYR][DECEMBER APPROPRIATIONS]-[FINMGT][FINEYR][DECEMBER EXPENDITURES]-[FINMGT][FINEYR][DECEMBER DISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED EXPENDITURES]-[FINMGT][FINEYR][DECEMBER ENCUMBRANCES]-[FINMGT][FINEYR][DECEMBER DISTRIBUTED ENCUMBRANCES]-[FINMGT][FINEYR][DECEMBER UNDISTRIBUTED ENCUMBRANCES]");
	d->expression=stralloc(temp);
	memset(temp,0,15000);
	sprintf(temp,"This virtual field represents the December available balance which is calculated by the difference between the December posted appropriations and the sum of the December posted, distributed, and undistributed expenditures and encumbrances.");
	d->desc=stralloc(temp);
	memset(temp,0,15000);
	writeDFvirtual("FINMGT",d);
	if(d!=NULL) FreeDFvirtual(d);
}
